headers:
  - source: "**"
    headers:
      - key: "Cache-Control"
        value: "public, max-age=3600"
rewrites:
  - source: "**"
    destination: "/index.html" # Important for single-page applications
hosting:
  public: "./dist" # Point this to your Vite build output directory

